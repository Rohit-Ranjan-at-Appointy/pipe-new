node {
 	properties([parameters([choice(choices: ['service1', 'service2','service3', 'service4','service5', 'service6','service7', 'service8','service9', 'service10','service11', 'service12','service13', 'service14','service15', 'service16','service17', 'service18','service19', 'service20','service21', 'service22','service23', 'service24','service25', 'service26','service27', 'service28','service29', 'service30'], description: '', name: 'Choises')])])
 	if (Choises.equals("service1")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service1/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v1:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
 	else if (Choises.equals("service2")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service2/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
  else if (Choises.equals("service3")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service3/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
  else if (Choises.equals("service4")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service4/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
  else if (Choises.equals("service5")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service5/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
  else if (Choises.equals("service6")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service6/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
  else if (Choises.equals("service7")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service7/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
  else if (Choises.equals("service8")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service8/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
  else if (Choises.equals("service9")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service9/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
  else if (Choises.equals("service10")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service10/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
  else if (Choises.equals("service11")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service11/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
  else if (Choises.equals("service12")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service12/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
  else if (Choises.equals("service13")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service13/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
  else if (Choises.equals("service14")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service14/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
  else if (Choises.equals("service15")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service15/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
  else if (Choises.equals("service16")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service16/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
  else if (Choises.equals("service17")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service17/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
  else if (Choises.equals("service18")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service18/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
  } 
  else if (Choises.equals("service19")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service19/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
  }
  else if (Choises.equals("service20")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service20/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
  else if (Choises.equals("service21")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service21/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
  else if (Choises.equals("service22")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service22/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
 else if (Choises.equals("service23")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service23/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
  else if (Choises.equals("service24")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service24/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
  else if (Choises.equals("service25")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service25/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
  else if (Choises.equals("service26")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service26/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
  else if (Choises.equals("service27")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service27/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
  else if (Choises.equals("service28")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service28/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
  else if (Choises.equals("service29")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service29/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
 	else if (Choises.equals("service30")){
 	checkout scm
 	docker.withRegistry('https://registry.hub.docker.com', 'dockerhub') {
 	def dockerfile = '/var/lib/jenkins/workspace/Docker-pipeline/service30/Dockerfile .'
 	def customImage = docker.build("qwerty0901/para-v2:${env.BUILD_ID}", "-f ${dockerfile}")
 	 
 	/* Push the container to the custom Registry */
 	customImage.push()
 	}
 	}
